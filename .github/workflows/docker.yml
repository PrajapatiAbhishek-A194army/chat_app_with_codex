name: Build and Push Images

on:
  push:
    branches:
      - main

jobs:

  docker:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v4

      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Backend
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/chatapp-backend:latest ./backend

      - name: Push Backend
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/chatapp-backend:latest

      - name: Build Frontend
        run: |
          docker build \
          --build-arg VITE_API_BASE_URL="${{ secrets.VITE_API_BASE_URL }}" \
          --build-arg VITE_GOOGLE_CLIENT_ID="${{ secrets.VITE_GOOGLE_CLIENT_ID }}" \
          -t ${{ secrets.DOCKER_USERNAME }}/chatapp-frontend:latest \
          ./frontend  

      - name: Push Frontend
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/chatapp-frontend:latest